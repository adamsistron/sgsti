<?php

/**
 * Base static class for performing query and update operations on the 'j825t_matriz_riesgo' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Sat Nov 14 07:33:32 2015
 *
 * @package    lib.model.om
 */
abstract class BaseJ825tMatrizRiesgoPeer {

	/** the default database name for this class */
	const DATABASE_NAME = 'propel';

	/** the table name for this class */
	const TABLE_NAME = 'j825t_matriz_riesgo';

	/** A class that can be returned by this peer. */
	const CLASS_DEFAULT = 'lib.model.J825tMatrizRiesgo';

	/** The total number of columns. */
	const NUM_COLUMNS = 8;

	/** The number of lazy-loaded columns. */
	const NUM_LAZY_LOAD_COLUMNS = 0;

	/** the column name for the CO_MATRIZ_RIESGO field */
	const CO_MATRIZ_RIESGO = 'j825t_matriz_riesgo.CO_MATRIZ_RIESGO';

	/** the column name for the CO_IMPACTO field */
	const CO_IMPACTO = 'j825t_matriz_riesgo.CO_IMPACTO';

	/** the column name for the CO_AMENAZA field */
	const CO_AMENAZA = 'j825t_matriz_riesgo.CO_AMENAZA';

	/** the column name for the CO_RIESGO field */
	const CO_RIESGO = 'j825t_matriz_riesgo.CO_RIESGO';

	/** the column name for the CREATED_AT field */
	const CREATED_AT = 'j825t_matriz_riesgo.CREATED_AT';

	/** the column name for the UPDATED_AT field */
	const UPDATED_AT = 'j825t_matriz_riesgo.UPDATED_AT';

	/** the column name for the CO_CREATED_AT field */
	const CO_CREATED_AT = 'j825t_matriz_riesgo.CO_CREATED_AT';

	/** the column name for the CO_UPDATED_AT field */
	const CO_UPDATED_AT = 'j825t_matriz_riesgo.CO_UPDATED_AT';

	/**
	 * An identiy map to hold any loaded instances of J825tMatrizRiesgo objects.
	 * This must be public so that other peer classes can access this when hydrating from JOIN
	 * queries.
	 * @var        array J825tMatrizRiesgo[]
	 */
	public static $instances = array();

	/**
	 * The MapBuilder instance for this peer.
	 * @var        MapBuilder
	 */
	private static $mapBuilder = null;

	/**
	 * holds an array of fieldnames
	 *
	 * first dimension keys are the type constants
	 * e.g. self::$fieldNames[self::TYPE_PHPNAME][0] = 'Id'
	 */
	private static $fieldNames = array (
		BasePeer::TYPE_PHPNAME => array ('CoMatrizRiesgo', 'CoImpacto', 'CoAmenaza', 'CoRiesgo', 'CreatedAt', 'UpdatedAt', 'CoCreatedAt', 'CoUpdatedAt', ),
		BasePeer::TYPE_STUDLYPHPNAME => array ('coMatrizRiesgo', 'coImpacto', 'coAmenaza', 'coRiesgo', 'createdAt', 'updatedAt', 'coCreatedAt', 'coUpdatedAt', ),
		BasePeer::TYPE_COLNAME => array (self::CO_MATRIZ_RIESGO, self::CO_IMPACTO, self::CO_AMENAZA, self::CO_RIESGO, self::CREATED_AT, self::UPDATED_AT, self::CO_CREATED_AT, self::CO_UPDATED_AT, ),
		BasePeer::TYPE_FIELDNAME => array ('co_matriz_riesgo', 'co_impacto', 'co_amenaza', 'co_riesgo', 'created_at', 'updated_at', 'co_created_at', 'co_updated_at', ),
		BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, 7, )
	);

	/**
	 * holds an array of keys for quick access to the fieldnames array
	 *
	 * first dimension keys are the type constants
	 * e.g. self::$fieldNames[BasePeer::TYPE_PHPNAME]['Id'] = 0
	 */
	private static $fieldKeys = array (
		BasePeer::TYPE_PHPNAME => array ('CoMatrizRiesgo' => 0, 'CoImpacto' => 1, 'CoAmenaza' => 2, 'CoRiesgo' => 3, 'CreatedAt' => 4, 'UpdatedAt' => 5, 'CoCreatedAt' => 6, 'CoUpdatedAt' => 7, ),
		BasePeer::TYPE_STUDLYPHPNAME => array ('coMatrizRiesgo' => 0, 'coImpacto' => 1, 'coAmenaza' => 2, 'coRiesgo' => 3, 'createdAt' => 4, 'updatedAt' => 5, 'coCreatedAt' => 6, 'coUpdatedAt' => 7, ),
		BasePeer::TYPE_COLNAME => array (self::CO_MATRIZ_RIESGO => 0, self::CO_IMPACTO => 1, self::CO_AMENAZA => 2, self::CO_RIESGO => 3, self::CREATED_AT => 4, self::UPDATED_AT => 5, self::CO_CREATED_AT => 6, self::CO_UPDATED_AT => 7, ),
		BasePeer::TYPE_FIELDNAME => array ('co_matriz_riesgo' => 0, 'co_impacto' => 1, 'co_amenaza' => 2, 'co_riesgo' => 3, 'created_at' => 4, 'updated_at' => 5, 'co_created_at' => 6, 'co_updated_at' => 7, ),
		BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, 7, )
	);

	/**
	 * Get a (singleton) instance of the MapBuilder for this peer class.
	 * @return     MapBuilder The map builder for this peer
	 */
	public static function getMapBuilder()
	{
		if (self::$mapBuilder === null) {
			self::$mapBuilder = new J825tMatrizRiesgoMapBuilder();
		}
		return self::$mapBuilder;
	}
	/**
	 * Translates a fieldname to another type
	 *
	 * @param      string $name field name
	 * @param      string $fromType One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                         BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @param      string $toType   One of the class type constants
	 * @return     string translated name of the field.
	 * @throws     PropelException - if the specified name could not be found in the fieldname mappings.
	 */
	static public function translateFieldName($name, $fromType, $toType)
	{
		$toNames = self::getFieldNames($toType);
		$key = isset(self::$fieldKeys[$fromType][$name]) ? self::$fieldKeys[$fromType][$name] : null;
		if ($key === null) {
			throw new PropelException("'$name' could not be found in the field names of type '$fromType'. These are: " . print_r(self::$fieldKeys[$fromType], true));
		}
		return $toNames[$key];
	}

	/**
	 * Returns an array of field names.
	 *
	 * @param      string $type The type of fieldnames to return:
	 *                      One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                      BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     array A list of field names
	 */

	static public function getFieldNames($type = BasePeer::TYPE_PHPNAME)
	{
		if (!array_key_exists($type, self::$fieldNames)) {
			throw new PropelException('Method getFieldNames() expects the parameter $type to be one of the class constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME, BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. ' . $type . ' was given.');
		}
		return self::$fieldNames[$type];
	}

	/**
	 * Convenience method which changes table.column to alias.column.
	 *
	 * Using this method you can maintain SQL abstraction while using column aliases.
	 * <code>
	 *		$c->addAlias("alias1", TablePeer::TABLE_NAME);
	 *		$c->addJoin(TablePeer::alias("alias1", TablePeer::PRIMARY_KEY_COLUMN), TablePeer::PRIMARY_KEY_COLUMN);
	 * </code>
	 * @param      string $alias The alias for the current table.
	 * @param      string $column The column name for current table. (i.e. J825tMatrizRiesgoPeer::COLUMN_NAME).
	 * @return     string
	 */
	public static function alias($alias, $column)
	{
		return str_replace(J825tMatrizRiesgoPeer::TABLE_NAME.'.', $alias.'.', $column);
	}

	/**
	 * Add all the columns needed to create a new object.
	 *
	 * Note: any columns that were marked with lazyLoad="true" in the
	 * XML schema will not be added to the select list and only loaded
	 * on demand.
	 *
	 * @param      criteria object containing the columns to add.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function addSelectColumns(Criteria $criteria)
	{

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO);

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::CO_IMPACTO);

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::CO_AMENAZA);

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::CO_RIESGO);

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::CREATED_AT);

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::UPDATED_AT);

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::CO_CREATED_AT);

		$criteria->addSelectColumn(J825tMatrizRiesgoPeer::CO_UPDATED_AT);

	}

	/**
	 * Returns the number of rows matching criteria.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @return     int Number of matching rows.
	 */
	public static function doCount(Criteria $criteria, $distinct = false, PropelPDO $con = null)
	{
		// we may modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(J825tMatrizRiesgoPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		$criteria->setDbName(self::DATABASE_NAME); // Set the correct dbName

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}


    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		// BasePeer returns a PDOStatement
		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}
	/**
	 * Method to select one object from the DB.
	 *
	 * @param      Criteria $criteria object used to create the SELECT statement.
	 * @param      PropelPDO $con
	 * @return     J825tMatrizRiesgo
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectOne(Criteria $criteria, PropelPDO $con = null)
	{
		$critcopy = clone $criteria;
		$critcopy->setLimit(1);
		$objects = J825tMatrizRiesgoPeer::doSelect($critcopy, $con);
		if ($objects) {
			return $objects[0];
		}
		return null;
	}
	/**
	 * Method to do selects.
	 *
	 * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
	 * @param      PropelPDO $con
	 * @return     array Array of selected Objects
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelect(Criteria $criteria, PropelPDO $con = null)
	{
		return J825tMatrizRiesgoPeer::populateObjects(J825tMatrizRiesgoPeer::doSelectStmt($criteria, $con));
	}
	/**
	 * Prepares the Criteria object and uses the parent doSelect() method to execute a PDOStatement.
	 *
	 * Use this method directly if you want to work with an executed statement durirectly (for example
	 * to perform your own object hydration).
	 *
	 * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
	 * @param      PropelPDO $con The connection to use
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 * @return     PDOStatement The executed PDOStatement object.
	 * @see        BasePeer::doSelect()
	 */
	public static function doSelectStmt(Criteria $criteria, PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doSelectStmt:doSelectStmt') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		if (!$criteria->hasSelectClause()) {
			$criteria = clone $criteria;
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		// BasePeer returns a PDOStatement
		return BasePeer::doSelect($criteria, $con);
	}
	/**
	 * Adds an object to the instance pool.
	 *
	 * Propel keeps cached copies of objects in an instance pool when they are retrieved
	 * from the database.  In some cases -- especially when you override doSelect*()
	 * methods in your stub classes -- you may need to explicitly add objects
	 * to the cache in order to ensure that the same objects are always returned by doSelect*()
	 * and retrieveByPK*() calls.
	 *
	 * @param      J825tMatrizRiesgo $value A J825tMatrizRiesgo object.
	 * @param      string $key (optional) key to use for instance map (for performance boost if key was already calculated externally).
	 */
	public static function addInstanceToPool(J825tMatrizRiesgo $obj, $key = null)
	{
		if (Propel::isInstancePoolingEnabled()) {
			if ($key === null) {
				$key = (string) $obj->getCoMatrizRiesgo();
			} // if key === null
			self::$instances[$key] = $obj;
		}
	}

	/**
	 * Removes an object from the instance pool.
	 *
	 * Propel keeps cached copies of objects in an instance pool when they are retrieved
	 * from the database.  In some cases -- especially when you override doDelete
	 * methods in your stub classes -- you may need to explicitly remove objects
	 * from the cache in order to prevent returning objects that no longer exist.
	 *
	 * @param      mixed $value A J825tMatrizRiesgo object or a primary key value.
	 */
	public static function removeInstanceFromPool($value)
	{
		if (Propel::isInstancePoolingEnabled() && $value !== null) {
			if (is_object($value) && $value instanceof J825tMatrizRiesgo) {
				$key = (string) $value->getCoMatrizRiesgo();
			} elseif (is_scalar($value)) {
				// assume we've been passed a primary key
				$key = (string) $value;
			} else {
				$e = new PropelException("Invalid value passed to removeInstanceFromPool().  Expected primary key or J825tMatrizRiesgo object; got " . (is_object($value) ? get_class($value) . ' object.' : var_export($value,true)));
				throw $e;
			}

			unset(self::$instances[$key]);
		}
	} // removeInstanceFromPool()

	/**
	 * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
	 *
	 * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
	 * a multi-column primary key, a serialize()d version of the primary key will be returned.
	 *
	 * @param      string $key The key (@see getPrimaryKeyHash()) for this instance.
	 * @return     J825tMatrizRiesgo Found object or NULL if 1) no instance exists for specified key or 2) instance pooling has been disabled.
	 * @see        getPrimaryKeyHash()
	 */
	public static function getInstanceFromPool($key)
	{
		if (Propel::isInstancePoolingEnabled()) {
			if (isset(self::$instances[$key])) {
				return self::$instances[$key];
			}
		}
		return null; // just to be explicit
	}
	
	/**
	 * Clear the instance pool.
	 *
	 * @return     void
	 */
	public static function clearInstancePool()
	{
		self::$instances = array();
	}
	
	/**
	 * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
	 *
	 * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
	 * a multi-column primary key, a serialize()d version of the primary key will be returned.
	 *
	 * @param      array $row PropelPDO resultset row.
	 * @param      int $startcol The 0-based offset for reading from the resultset row.
	 * @return     string A string version of PK or NULL if the components of primary key in result array are all null.
	 */
	public static function getPrimaryKeyHashFromRow($row, $startcol = 0)
	{
		// If the PK cannot be derived from the row, return NULL.
		if ($row[$startcol + 0] === null) {
			return null;
		}
		return (string) $row[$startcol + 0];
	}

	/**
	 * The returned array will contain objects of the default type or
	 * objects that inherit from the default.
	 *
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function populateObjects(PDOStatement $stmt)
	{
		$results = array();
	
		// set the class once to avoid overhead in the loop
		$cls = J825tMatrizRiesgoPeer::getOMClass();
		$cls = substr('.'.$cls, strrpos('.'.$cls, '.') + 1);
		// populate the object(s)
		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj = J825tMatrizRiesgoPeer::getInstanceFromPool($key))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj->hydrate($row, 0, true); // rehydrate
				$results[] = $obj;
			} else {
		
				$obj = new $cls();
				$obj->hydrate($row);
				$results[] = $obj;
				J825tMatrizRiesgoPeer::addInstanceToPool($obj, $key);
			} // if key exists
		}
		$stmt->closeCursor();
		return $results;
	}

	/**
	 * Returns the number of rows matching criteria, joining the related J823tNivelImpacto table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinJ823tNivelImpacto(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(J825tMatrizRiesgoPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);


    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related J824tNivelAmenaza table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinJ824tNivelAmenaza(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(J825tMatrizRiesgoPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);


    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related J822tNivelRiesgo table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinJ822tNivelRiesgo(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(J825tMatrizRiesgoPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);


    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Selects a collection of J825tMatrizRiesgo objects pre-filled with their J823tNivelImpacto objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of J825tMatrizRiesgo objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinJ823tNivelImpacto(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doSelectJoin:doSelectJoin') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $c, $con);
    }


		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		J825tMatrizRiesgoPeer::addSelectColumns($c);
		$startcol = (J825tMatrizRiesgoPeer::NUM_COLUMNS - J825tMatrizRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);
		J823tNivelImpactoPeer::addSelectColumns($c);

		$c->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = J825tMatrizRiesgoPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = J825tMatrizRiesgoPeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				J825tMatrizRiesgoPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = J823tNivelImpactoPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = J823tNivelImpactoPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = J823tNivelImpactoPeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					J823tNivelImpactoPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to $obj2 (J823tNivelImpacto)
				$obj2->addJ825tMatrizRiesgo($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of J825tMatrizRiesgo objects pre-filled with their J824tNivelAmenaza objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of J825tMatrizRiesgo objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinJ824tNivelAmenaza(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		J825tMatrizRiesgoPeer::addSelectColumns($c);
		$startcol = (J825tMatrizRiesgoPeer::NUM_COLUMNS - J825tMatrizRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);
		J824tNivelAmenazaPeer::addSelectColumns($c);

		$c->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = J825tMatrizRiesgoPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = J825tMatrizRiesgoPeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				J825tMatrizRiesgoPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = J824tNivelAmenazaPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = J824tNivelAmenazaPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = J824tNivelAmenazaPeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					J824tNivelAmenazaPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to $obj2 (J824tNivelAmenaza)
				$obj2->addJ825tMatrizRiesgo($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of J825tMatrizRiesgo objects pre-filled with their J822tNivelRiesgo objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of J825tMatrizRiesgo objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinJ822tNivelRiesgo(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		J825tMatrizRiesgoPeer::addSelectColumns($c);
		$startcol = (J825tMatrizRiesgoPeer::NUM_COLUMNS - J825tMatrizRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);
		J822tNivelRiesgoPeer::addSelectColumns($c);

		$c->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = J825tMatrizRiesgoPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = J825tMatrizRiesgoPeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				J825tMatrizRiesgoPeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = J822tNivelRiesgoPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = J822tNivelRiesgoPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = J822tNivelRiesgoPeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					J822tNivelRiesgoPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to $obj2 (J822tNivelRiesgo)
				$obj2->addJ825tMatrizRiesgo($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Returns the number of rows matching criteria, joining all related tables
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAll(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(J825tMatrizRiesgoPeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);
		$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);
		$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}

	/**
	 * Selects a collection of J825tMatrizRiesgo objects pre-filled with all related objects.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of J825tMatrizRiesgo objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAll(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doSelectJoinAll:doSelectJoinAll') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $c, $con);
    }


		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		J825tMatrizRiesgoPeer::addSelectColumns($c);
		$startcol2 = (J825tMatrizRiesgoPeer::NUM_COLUMNS - J825tMatrizRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);

		J823tNivelImpactoPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (J823tNivelImpactoPeer::NUM_COLUMNS - J823tNivelImpactoPeer::NUM_LAZY_LOAD_COLUMNS);

		J824tNivelAmenazaPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (J824tNivelAmenazaPeer::NUM_COLUMNS - J824tNivelAmenazaPeer::NUM_LAZY_LOAD_COLUMNS);

		J822tNivelRiesgoPeer::addSelectColumns($c);
		$startcol5 = $startcol4 + (J822tNivelRiesgoPeer::NUM_COLUMNS - J822tNivelRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);

		$c->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);
		$c->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);
		$c->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = J825tMatrizRiesgoPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = J825tMatrizRiesgoPeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				J825tMatrizRiesgoPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

			// Add objects for joined J823tNivelImpacto rows

			$key2 = J823tNivelImpactoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
			if ($key2 !== null) {
				$obj2 = J823tNivelImpactoPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = J823tNivelImpactoPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					J823tNivelImpactoPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj2 (J823tNivelImpacto)
				$obj2->addJ825tMatrizRiesgo($obj1);
			} // if joined row not null

			// Add objects for joined J824tNivelAmenaza rows

			$key3 = J824tNivelAmenazaPeer::getPrimaryKeyHashFromRow($row, $startcol3);
			if ($key3 !== null) {
				$obj3 = J824tNivelAmenazaPeer::getInstanceFromPool($key3);
				if (!$obj3) {

					$omClass = J824tNivelAmenazaPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					J824tNivelAmenazaPeer::addInstanceToPool($obj3, $key3);
				} // if obj3 loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj3 (J824tNivelAmenaza)
				$obj3->addJ825tMatrizRiesgo($obj1);
			} // if joined row not null

			// Add objects for joined J822tNivelRiesgo rows

			$key4 = J822tNivelRiesgoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
			if ($key4 !== null) {
				$obj4 = J822tNivelRiesgoPeer::getInstanceFromPool($key4);
				if (!$obj4) {

					$omClass = J822tNivelRiesgoPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					J822tNivelRiesgoPeer::addInstanceToPool($obj4, $key4);
				} // if obj4 loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj4 (J822tNivelRiesgo)
				$obj4->addJ825tMatrizRiesgo($obj1);
			} // if joined row not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related J823tNivelImpacto table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptJ823tNivelImpacto(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);
				$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related J824tNivelAmenaza table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptJ824tNivelAmenaza(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);
				$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related J822tNivelRiesgo table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptJ822tNivelRiesgo(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			J825tMatrizRiesgoPeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);
				$criteria->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Selects a collection of J825tMatrizRiesgo objects pre-filled with all related objects except J823tNivelImpacto.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of J825tMatrizRiesgo objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptJ823tNivelImpacto(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doSelectJoinAllExcept:doSelectJoinAllExcept') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $c, $con);
    }


		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		J825tMatrizRiesgoPeer::addSelectColumns($c);
		$startcol2 = (J825tMatrizRiesgoPeer::NUM_COLUMNS - J825tMatrizRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);

		J824tNivelAmenazaPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (J824tNivelAmenazaPeer::NUM_COLUMNS - J824tNivelAmenazaPeer::NUM_LAZY_LOAD_COLUMNS);

		J822tNivelRiesgoPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (J822tNivelRiesgoPeer::NUM_COLUMNS - J822tNivelRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);
				$c->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = J825tMatrizRiesgoPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = J825tMatrizRiesgoPeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				J825tMatrizRiesgoPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined J824tNivelAmenaza rows

				$key2 = J824tNivelAmenazaPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = J824tNivelAmenazaPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = J824tNivelAmenazaPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					J824tNivelAmenazaPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj2 (J824tNivelAmenaza)
				$obj2->addJ825tMatrizRiesgo($obj1);

			} // if joined row is not null

				// Add objects for joined J822tNivelRiesgo rows

				$key3 = J822tNivelRiesgoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = J822tNivelRiesgoPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = J822tNivelRiesgoPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					J822tNivelRiesgoPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj3 (J822tNivelRiesgo)
				$obj3->addJ825tMatrizRiesgo($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of J825tMatrizRiesgo objects pre-filled with all related objects except J824tNivelAmenaza.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of J825tMatrizRiesgo objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptJ824tNivelAmenaza(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		J825tMatrizRiesgoPeer::addSelectColumns($c);
		$startcol2 = (J825tMatrizRiesgoPeer::NUM_COLUMNS - J825tMatrizRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);

		J823tNivelImpactoPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (J823tNivelImpactoPeer::NUM_COLUMNS - J823tNivelImpactoPeer::NUM_LAZY_LOAD_COLUMNS);

		J822tNivelRiesgoPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (J822tNivelRiesgoPeer::NUM_COLUMNS - J822tNivelRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);
				$c->addJoin(array(J825tMatrizRiesgoPeer::CO_RIESGO,), array(J822tNivelRiesgoPeer::CO_NIVEL_RIESGO,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = J825tMatrizRiesgoPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = J825tMatrizRiesgoPeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				J825tMatrizRiesgoPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined J823tNivelImpacto rows

				$key2 = J823tNivelImpactoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = J823tNivelImpactoPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = J823tNivelImpactoPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					J823tNivelImpactoPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj2 (J823tNivelImpacto)
				$obj2->addJ825tMatrizRiesgo($obj1);

			} // if joined row is not null

				// Add objects for joined J822tNivelRiesgo rows

				$key3 = J822tNivelRiesgoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = J822tNivelRiesgoPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = J822tNivelRiesgoPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					J822tNivelRiesgoPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj3 (J822tNivelRiesgo)
				$obj3->addJ825tMatrizRiesgo($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of J825tMatrizRiesgo objects pre-filled with all related objects except J822tNivelRiesgo.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of J825tMatrizRiesgo objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptJ822tNivelRiesgo(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		J825tMatrizRiesgoPeer::addSelectColumns($c);
		$startcol2 = (J825tMatrizRiesgoPeer::NUM_COLUMNS - J825tMatrizRiesgoPeer::NUM_LAZY_LOAD_COLUMNS);

		J823tNivelImpactoPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (J823tNivelImpactoPeer::NUM_COLUMNS - J823tNivelImpactoPeer::NUM_LAZY_LOAD_COLUMNS);

		J824tNivelAmenazaPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (J824tNivelAmenazaPeer::NUM_COLUMNS - J824tNivelAmenazaPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(J825tMatrizRiesgoPeer::CO_IMPACTO,), array(J823tNivelImpactoPeer::CO_NIVEL_IMPACTO,), $join_behavior);
				$c->addJoin(array(J825tMatrizRiesgoPeer::CO_AMENAZA,), array(J824tNivelAmenazaPeer::CO_NIVEL_AMENAZA,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = J825tMatrizRiesgoPeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = J825tMatrizRiesgoPeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = J825tMatrizRiesgoPeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				J825tMatrizRiesgoPeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined J823tNivelImpacto rows

				$key2 = J823tNivelImpactoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = J823tNivelImpactoPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = J823tNivelImpactoPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					J823tNivelImpactoPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj2 (J823tNivelImpacto)
				$obj2->addJ825tMatrizRiesgo($obj1);

			} // if joined row is not null

				// Add objects for joined J824tNivelAmenaza rows

				$key3 = J824tNivelAmenazaPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = J824tNivelAmenazaPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = J824tNivelAmenazaPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					J824tNivelAmenazaPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (J825tMatrizRiesgo) to the collection in $obj3 (J824tNivelAmenaza)
				$obj3->addJ825tMatrizRiesgo($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


  static public function getUniqueColumnNames()
  {
    return array();
  }
	/**
	 * Returns the TableMap related to this peer.
	 * This method is not needed for general use but a specific application could have a need.
	 * @return     TableMap
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function getTableMap()
	{
		return Propel::getDatabaseMap(self::DATABASE_NAME)->getTable(self::TABLE_NAME);
	}

	/**
	 * The class that the Peer will make instances of.
	 *
	 * This uses a dot-path notation which is tranalted into a path
	 * relative to a location on the PHP include_path.
	 * (e.g. path.to.MyClass -> 'path/to/MyClass.php')
	 *
	 * @return     string path.to.ClassName
	 */
	public static function getOMClass()
	{
		return J825tMatrizRiesgoPeer::CLASS_DEFAULT;
	}

	/**
	 * Method perform an INSERT on the database, given a J825tMatrizRiesgo or Criteria object.
	 *
	 * @param      mixed $values Criteria or J825tMatrizRiesgo object containing data that is used to create the INSERT statement.
	 * @param      PropelPDO $con the PropelPDO connection to use
	 * @return     mixed The new primary key.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doInsert($values, PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doInsert:pre') as $callable)
    {
      $ret = call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $values, $con);
      if (false !== $ret)
      {
        return $ret;
      }
    }


		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		if ($values instanceof Criteria) {
			$criteria = clone $values; // rename for clarity
		} else {
			$criteria = $values->buildCriteria(); // build Criteria from J825tMatrizRiesgo object
		}

		if ($criteria->containsKey(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO) && $criteria->keyContainsValue(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO) ) {
			throw new PropelException('Cannot insert a value for auto-increment primary key ('.J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO.')');
		}


		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		try {
			// use transaction because $criteria could contain info
			// for more than one table (I guess, conceivably)
			$con->beginTransaction();
			$pk = BasePeer::doInsert($criteria, $con);
			$con->commit();
		} catch(PropelException $e) {
			$con->rollBack();
			throw $e;
		}

		
    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doInsert:post') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $values, $con, $pk);
    }

    return $pk;
	}

	/**
	 * Method perform an UPDATE on the database, given a J825tMatrizRiesgo or Criteria object.
	 *
	 * @param      mixed $values Criteria or J825tMatrizRiesgo object containing data that is used to create the UPDATE statement.
	 * @param      PropelPDO $con The connection to use (specify PropelPDO connection object to exert more control over transactions).
	 * @return     int The number of affected rows (if supported by underlying database driver).
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doUpdate($values, PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doUpdate:pre') as $callable)
    {
      $ret = call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $values, $con);
      if (false !== $ret)
      {
        return $ret;
      }
    }


		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$selectCriteria = new Criteria(self::DATABASE_NAME);

		if ($values instanceof Criteria) {
			$criteria = clone $values; // rename for clarity

			$comparison = $criteria->getComparison(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO);
			$selectCriteria->add(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO, $criteria->remove(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO), $comparison);

		} else { // $values is J825tMatrizRiesgo object
			$criteria = $values->buildCriteria(); // gets full criteria
			$selectCriteria = $values->buildPkeyCriteria(); // gets criteria w/ primary key(s)
		}

		// set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		$ret = BasePeer::doUpdate($selectCriteria, $criteria, $con);
	

    foreach (sfMixer::getCallables('BaseJ825tMatrizRiesgoPeer:doUpdate:post') as $callable)
    {
      call_user_func($callable, 'BaseJ825tMatrizRiesgoPeer', $values, $con, $ret);
    }

    return $ret;
  }

	/**
	 * Method to DELETE all rows from the j825t_matriz_riesgo table.
	 *
	 * @return     int The number of affected rows (if supported by underlying database driver).
	 */
	public static function doDeleteAll($con = null)
	{
		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		$affectedRows = 0; // initialize var to track total num of affected rows
		try {
			// use transaction because $criteria could contain info
			// for more than one table or we could emulating ON DELETE CASCADE, etc.
			$con->beginTransaction();
			$affectedRows += BasePeer::doDeleteAll(J825tMatrizRiesgoPeer::TABLE_NAME, $con);
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Method perform a DELETE on the database, given a J825tMatrizRiesgo or Criteria object OR a primary key value.
	 *
	 * @param      mixed $values Criteria or J825tMatrizRiesgo object or primary key or array of primary keys
	 *              which is used to create the DELETE statement
	 * @param      PropelPDO $con the connection to use
	 * @return     int 	The number of affected rows (if supported by underlying database driver).  This includes CASCADE-related rows
	 *				if supported by native driver or if emulated using Propel.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	 public static function doDelete($values, PropelPDO $con = null)
	 {
		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		if ($values instanceof Criteria) {
			// invalidate the cache for all objects of this type, since we have no
			// way of knowing (without running a query) what objects should be invalidated
			// from the cache based on this Criteria.
			J825tMatrizRiesgoPeer::clearInstancePool();

			// rename for clarity
			$criteria = clone $values;
		} elseif ($values instanceof J825tMatrizRiesgo) {
			// invalidate the cache for this single object
			J825tMatrizRiesgoPeer::removeInstanceFromPool($values);
			// create criteria based on pk values
			$criteria = $values->buildPkeyCriteria();
		} else {
			// it must be the primary key



			$criteria = new Criteria(self::DATABASE_NAME);
			$criteria->add(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO, (array) $values, Criteria::IN);

			foreach ((array) $values as $singleval) {
				// we can invalidate the cache for this single object
				J825tMatrizRiesgoPeer::removeInstanceFromPool($singleval);
			}
		}

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		$affectedRows = 0; // initialize var to track total num of affected rows

		try {
			// use transaction because $criteria could contain info
			// for more than one table or we could emulating ON DELETE CASCADE, etc.
			$con->beginTransaction();
			
			$affectedRows += BasePeer::doDelete($criteria, $con);

			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Validates all modified columns of given J825tMatrizRiesgo object.
	 * If parameter $columns is either a single column name or an array of column names
	 * than only those columns are validated.
	 *
	 * NOTICE: This does not apply to primary or foreign keys for now.
	 *
	 * @param      J825tMatrizRiesgo $obj The object to validate.
	 * @param      mixed $cols Column name or array of column names.
	 *
	 * @return     mixed TRUE if all columns are valid or the error message of the first invalid column.
	 */
	public static function doValidate(J825tMatrizRiesgo $obj, $cols = null)
	{
		$columns = array();

		if ($cols) {
			$dbMap = Propel::getDatabaseMap(J825tMatrizRiesgoPeer::DATABASE_NAME);
			$tableMap = $dbMap->getTable(J825tMatrizRiesgoPeer::TABLE_NAME);

			if (! is_array($cols)) {
				$cols = array($cols);
			}

			foreach ($cols as $colName) {
				if ($tableMap->containsColumn($colName)) {
					$get = 'get' . $tableMap->getColumn($colName)->getPhpName();
					$columns[$colName] = $obj->$get();
				}
			}
		} else {

		}

		$res =  BasePeer::doValidate(J825tMatrizRiesgoPeer::DATABASE_NAME, J825tMatrizRiesgoPeer::TABLE_NAME, $columns);
    if ($res !== true) {
        $request = sfContext::getInstance()->getRequest();
        foreach ($res as $failed) {
            $col = J825tMatrizRiesgoPeer::translateFieldname($failed->getColumn(), BasePeer::TYPE_COLNAME, BasePeer::TYPE_PHPNAME);
        }
    }

    return $res;
	}

	/**
	 * Retrieve a single object by pkey.
	 *
	 * @param      string $pk the primary key.
	 * @param      PropelPDO $con the connection to use
	 * @return     J825tMatrizRiesgo
	 */
	public static function retrieveByPK($pk, PropelPDO $con = null)
	{

		if (null !== ($obj = J825tMatrizRiesgoPeer::getInstanceFromPool((string) $pk))) {
			return $obj;
		}

		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria = new Criteria(J825tMatrizRiesgoPeer::DATABASE_NAME);
		$criteria->add(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO, $pk);

		$v = J825tMatrizRiesgoPeer::doSelect($criteria, $con);

		return !empty($v) > 0 ? $v[0] : null;
	}

	/**
	 * Retrieve multiple objects by pkey.
	 *
	 * @param      array $pks List of primary keys
	 * @param      PropelPDO $con the connection to use
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function retrieveByPKs($pks, PropelPDO $con = null)
	{
		if ($con === null) {
			$con = Propel::getConnection(J825tMatrizRiesgoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$objs = null;
		if (empty($pks)) {
			$objs = array();
		} else {
			$criteria = new Criteria(J825tMatrizRiesgoPeer::DATABASE_NAME);
			$criteria->add(J825tMatrizRiesgoPeer::CO_MATRIZ_RIESGO, $pks, Criteria::IN);
			$objs = J825tMatrizRiesgoPeer::doSelect($criteria, $con);
		}
		return $objs;
	}

} // BaseJ825tMatrizRiesgoPeer

// This is the static code needed to register the MapBuilder for this table with the main Propel class.
//
// NOTE: This static code cannot call methods on the J825tMatrizRiesgoPeer class, because it is not defined yet.
// If you need to use overridden methods, you can add this code to the bottom of the J825tMatrizRiesgoPeer class:
//
// Propel::getDatabaseMap(J825tMatrizRiesgoPeer::DATABASE_NAME)->addTableBuilder(J825tMatrizRiesgoPeer::TABLE_NAME, J825tMatrizRiesgoPeer::getMapBuilder());
//
// Doing so will effectively overwrite the registration below.

Propel::getDatabaseMap(BaseJ825tMatrizRiesgoPeer::DATABASE_NAME)->addTableBuilder(BaseJ825tMatrizRiesgoPeer::TABLE_NAME, BaseJ825tMatrizRiesgoPeer::getMapBuilder());

